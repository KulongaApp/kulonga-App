// Aqui usamos o `expo-router` para carregar a árvore de rotas na pasta /app
// O `expo-router` gere o root do app automaticamente.
// TODO: se precisarmos de lógica de arranque personalizada, podemos usar um ficheiro separado.
// Usamos o `expo-router` como ponto de entrada do app.
// Isto exporta o componente root que o Expo regista automaticamente.
// TODO: adicionar lógica de arranque personalizada se necessário.
export { default } from 'expo-router/entry';
